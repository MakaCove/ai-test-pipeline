{
  "name": "ai-test-pipeline",
  "version": "2.0.0",
  "description": "面向 AI Agent 的测试流水线技能包：项目分析 -> API/UI 用例生成 -> 校验 -> 执行 -> Markdown 报告。",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "validate": "node scripts/validate-artifacts.mjs",
    "test:api": "node scripts/run-api-tests.mjs",
    "test:ui": "node scripts/run-ui-tests.mjs"
  },
  "optionalDependencies": {
    "playwright": "^1.49.0"
  }
}
