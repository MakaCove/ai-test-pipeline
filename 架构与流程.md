# AI 测试流水线 — 架构与流程图

> 本文描述 `ai-test-pipeline` 五技能的依赖关系与**数据流**。  
> Schema 以 `_shared/` 为唯一来源；验收看**覆盖率**，不看用例条数 KPI。

---

## 一、目标架构图

```mermaid
flowchart TB
    subgraph 用户["👤 用户"]
        U["说一句话触发"]
    end

    subgraph 技能层["🎯 五技能流水线"]
        direction TB
        S0["⓪ project-analyzer<br/>────────<br/>扫入口层代码<br/>输出索引地图"]
        S1["① api-test-case-generate<br/>────────<br/>读 Controller/DTO<br/>建 endpointContracts"]
        S2["② functional-test-case-generate<br/>────────<br/>读 Router/View<br/>建 featureInventory"]
        S3["③ api-test-execute<br/>────────<br/>HTTP 请求 + 断言"]
        S4["④ ui-test-execute<br/>────────<br/>浏览器自动化 + 截图"]
    end

    subgraph 产物层["📁 test-artifacts/"]
        direction TB
        A0["project-analysis-xxx.json<br/>地图：端点/路由/模块/auth"]
        A1["api-cases-xxx.json<br/>契约清单 + contractRef 用例"]
        A2["functional-cases-xxx.json<br/>功能清单 + featureRef 用例"]
        A3["api-report-xxx.md"]
        A4["ui-report-xxx.md"]
    end

    subgraph 共享层["📚 _shared/ 共享定义"]
        direction LR
        SH1["产物目录布局.md"]
        SH2["统一术语.md"]
        SH3["断言语法.md"]
        SH4["项目分析结构.md"]
        SH5["接口用例结构.md"]
        SH6["功能用例结构.md"]
        SH7["接口报告模板.md"]
        SH8["界面报告模板.md"]
    end

    U --> S0 & S1 & S2 & S3 & S4

    S0 --> A0
    A0 --> S1 & S2
    S1 --> A1
    S2 --> A2
    A1 --> S3 --> A3
    A2 --> S4 --> A4

    共享层 --> S0 & S1 & S2 & S3 & S4
```

### 各技能产出要点

| 技能 | 读什么 | 写什么 | 验收 |
|------|--------|--------|------|
| ⓪ project-analyzer | Controller、Router（入口层） | `project-analysis.json` | 端点/路由索引完整；含 `scanLimitations` |
| ① api-test-case-generate | analysis + **DTO/异常处理** | `api-cases.json` + `coverage.endpointContracts` | P0 契约场景 **100%**（`contractScenarioCoveragePercent`） |
| ② functional-test-case-generate | analysis + **View/组件** | `functional-cases.json` + `coverage.featureInventory` | P0 功能点 **100%**（`featureCoveragePercent`） |
| ③ api-test-execute | `api-cases.json` | `api-report.md` | 断言通过/失败明细 |
| ④ ui-test-execute | `functional-cases.json` | `ui-report.md` + 截图 | 断言通过/失败 + 截图 |

---

## 二、技能依赖图

```mermaid
flowchart LR
    subgraph 第零步["第零步：索引地图"]
        S0["⓪ project-analyzer"]
    end

    subgraph 第一步["第一步：清单 + 用例"]
        S1["① api-test-case-generate"]
        S2["② functional-test-case-generate"]
    end

    subgraph 第二步["第二步：执行"]
        S3["③ api-test-execute"]
        S4["④ ui-test-execute"]
    end

    SRC_BE["后端源码<br/>Controller / DTO / Handler"]
    SRC_FE["前端源码<br/>Router / View / Page"]

    S0 -->|"project-analysis.json<br/>端点 + 路由 + auth<br/>（地图，非契约）"| S1
    S0 -->|"同上"| S2

    SRC_BE -->|"必读，补全契约"| S1
    SRC_FE -->|"必读，补全功能点"| S2

    S1 -->|"api-cases.json<br/>endpointContracts<br/>contractRef/scenarioRef"| S3
    S2 -->|"functional-cases.json<br/>featureInventory<br/>featureRef/featureRefs"| S4

    S3 --> 终
    S4 --> 终

    S0 -.->|"可跳过；①/② 自行扫代码"| S1
    S0 -.->|"可跳过"| S2

    终["📋 测试报告"]
```

**说明**：

- `project-analysis` 是**加速器**，不是唯一输入；generate 技能**必须读源码**补全清单。
- 接口用例与功能用例**并行、互补**，数量无需对齐。
- 跳过 ⓪ 时，①/② 自行分析代码，但仍须产出 `endpointContracts` / `featureInventory`。

---

## 三、文件引用关系图

```mermaid
flowchart TB
    subgraph 技能["技能目录"]
        SKILL["SKILL.md"]
        F08["08_*驱动设计.md<br/>（generate 方法论核心）"]
        F02["02_执行工作流.md"]
        F04["04_输出规范.md"]
    end

    subgraph 共享["_shared/ Canonical Schema"]
        SH_PA["项目分析结构.md"]
        SH_API["接口用例结构.md"]
        SH_FUNC["功能用例结构.md"]
        SH_ASSERT["断言语法.md"]
        SH_ART["产物目录布局.md"]
        SH_RPT["*报告模板.md"]
    end

    SKILL --> F02 & F04 & F08

    F04 --> SH_PA & SH_API & SH_FUNC
    F02 --> SH_ASSERT & SH_ART & SH_RPT
    F08 -.->|"清单结构详见"| SH_API & SH_FUNC
```

| 技能 | 主要引用 _shared |
|------|------------------|
| project-analyzer | `项目分析结构.md`、`产物目录布局.md` |
| api-test-case-generate | `接口用例结构.md`、`断言语法.md` |
| functional-test-case-generate | `功能用例结构.md`、`断言语法.md` |
| api-test-execute | `接口用例结构.md`、`接口报告模板.md` |
| ui-test-execute | `功能用例结构.md`、`界面报告模板.md` |

---

## 四、单个技能内部文件结构

```mermaid
flowchart LR
    subgraph skill["技能目录（generate 类技能）"]
        S["SKILL.md"]
        S01["01_范围定义.md"]
        S02["02_执行工作流.md"]
        S03["03_设计规则.md"]
        S06["06_覆盖与验收标准.md"]
        S08["08_*驱动设计.md<br/>清单方法论"]
        S04["04_输出规范.md → _shared"]
    end

    S --> S01 & S02 & S03 & S06 & S08 & S04
    S08 --> S06
    S04 --> 共享["_shared/"]
```

---

## 五、数据流全景（三阶段）

```mermaid
flowchart TD
    subgraph 输入["📥 输入（源码为唯一真相）"]
        BE_CTRL["Controller / Handler / Router"]
        BE_DTO["DTO / Request Body / 校验注解"]
        BE_EX["GlobalExceptionHandler"]
        FE_RT["Router 配置"]
        FE_VIEW["View / Page 组件"]
        DOC["文档（可选，交叉验证）"]
    end

    subgraph 阶段A["阶段 A — 索引地图"]
        PA["project-analyzer"]
        PA_OUT["project-analysis.json<br/>modules[].endpoints[]<br/>modules[].routes[]<br/>auth + scanLimitations"]
    end

    subgraph 阶段B_API["阶段 B — 接口清单与用例"]
        GEN_API["api-test-case-generate"]
        INV_API["coverage.endpointContracts[]<br/>+ scenarios[]"]
        CASE_API["用例[]<br/>contractRef + scenarioRef"]
    end

    subgraph 阶段B_UI["阶段 B — 功能清单与用例"]
        GEN_UI["functional-test-case-generate"]
        INV_UI["coverage.featureInventory[]"]
        CASE_UI["用例[]<br/>featureRef / featureRefs"]
    end

    subgraph 阶段C["阶段 C — 执行与报告"]
        EXEC_API["api-test-execute → api-report.md"]
        EXEC_UI["ui-test-execute → ui-report.md"]
    end

    BE_CTRL --> PA
    FE_RT --> PA
    DOC -.-> PA
    PA --> PA_OUT

    PA_OUT --> GEN_API
    BE_CTRL --> GEN_API
    BE_DTO --> GEN_API
    BE_EX --> GEN_API
    GEN_API --> INV_API --> CASE_API

    PA_OUT --> GEN_UI
    FE_RT --> GEN_UI
    FE_VIEW --> GEN_UI
    GEN_UI --> INV_UI --> CASE_UI

    CASE_API --> EXEC_API
    CASE_UI --> EXEC_UI
```

### 阶段 A：索引地图（project-analysis）

| 写入字段 | 可信度 | 下游用途 |
|----------|--------|----------|
| `endpoints[].method/path` | 高 | 定位 Controller，建 `endpointContracts` |
| `endpoints[].requestBody` | 低 | **须读 DTO 核实** |
| `routes[].path/component` | 高 | 打开 View，建 `featureInventory` |
| `auth` | 中–高 | 登录前置、token 格式 |
| `scanLimitations` | — | 提醒勿把 analysis 当完整契约 |

### 阶段 B：清单 + 用例（generate）

**接口侧**

1. 对每个端点读 Controller + DTO → 写入 `coverage.endpointContracts[]`
2. 为每个契约场景（`scenarios[]`）写 1 条用例，绑定 `contractRef` + `scenarioRef`
3. 元信息统计 `contractScenarioCoveragePercent`，目标 **100%**

**功能侧**

1. 对每个路由打开 View → 写入 `coverage.featureInventory[]`
2. 为每个 P0 功能点写用例，绑定 `featureRef`（流程用例用 `featureRefs[]`）
3. 元信息统计 `featureCoveragePercent`，目标 **100%**

**禁止**：模板轮换凑数、`totalCases ≥ 2.5N` / `≥ 70` 等条数 KPI。

### 阶段 C：执行（execute）

- 读取 JSON 用例数组（跳过首元素 meta）
- 接口：变量替换 → HTTP → 断言（`断言语法.md`）
- UI：steps → 断言 → 截图
- 报告展示执行结果 + meta 中的覆盖率指标

---

## 六、产物字段追溯链

```mermaid
flowchart LR
    subgraph 分析产物
        E["modules[].endpoints[]"]
        R["modules[].routes[]"]
    end

    subgraph 接口链
        CTR["endpointContracts[].id<br/>CTR-*"]
        SCN["scenarios[].id<br/>SCN-*"]
        TC_API["用例 contractRef<br/>scenarioRef"]
    end

    subgraph 功能链
        FEAT["featureInventory[].id<br/>FEAT-*"]
        TC_FUNC["用例 featureRef<br/>featureRefs"]
    end

    E --> CTR --> SCN --> TC_API
    R --> FEAT --> TC_FUNC
```

| 层级 | 接口 | 功能 |
|------|------|------|
| 地图 | `endpoints[]` | `routes[]` |
| 清单 | `endpointContracts` + `scenarios` | `featureInventory` |
| 用例追溯 | `contractRef` + `scenarioRef` | `featureRef` / `featureRefs` |
| 验收指标 | `contractScenarioCoveragePercent` | `featureCoveragePercent` |

---

## 七、latest 指针与文件命名

```
test-artifacts/
├── project-analysis-{yyyyMMdd-HHmmss}.json
├── api-cases/api-cases-{yyyyMMdd-HHmmss}.json
├── functional-cases/functional-cases-{yyyyMMdd-HHmmss}.json
├── api-reports/api-report-{yyyyMMdd-HHmmss}.md
├── ui-reports/ui-report-{yyyyMMdd-HHmmss}.md
└── latest    ← 内容为最新时间戳字符串
```

每次生成新产物后更新 `latest`；下游技能优先读指针定位最新文件。详见 `产物目录布局.md`。

---

## 八、与 git-api-test-skill-pack 的关系

| 维度 | ai-test-pipeline（本包） | git-api-test-skill-pack（仓库内） |
|------|--------------------------|-----------------------------------|
| 范围 | 分析 → 接口+功能用例 → 双执行 | Git 变更驱动 → 接口测试为主 |
| 入口 | project-analyzer | Git diff / 提交记录 |
| 用例方法 | 契约/功能清单驱动 | RestAssured + Allure 流水线 |
| 适用 | 通用五技能流水线 | 本仓库 CI/变更回归场景 |

二者可独立使用；同一项目可同时保留，但**不要混用**旧模板脚本（如 `generate_test_cases.py`）与新清单驱动 Agent 工作流。
