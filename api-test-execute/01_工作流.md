# 工作流（API 执行）

1. 运行校验：`node scripts/validate-artifacts.mjs --type api`
2. 运行执行器：`node scripts/run-api-tests.mjs`
3. 查看报告：`test-artifacts/api-reports/`

## 失败处理

- 字段错误：修正 JSON 结构
- 依赖错误：检查 `trace.dependsOn`
- 变量错误：检查 `trace.consumes/produces` 与 `extract`
