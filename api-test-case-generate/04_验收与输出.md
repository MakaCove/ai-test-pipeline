# 验收与输出（API）

## 验收

- Schema 为 `ai-test-pipeline/api-case/v2`
- 每条用例包含 `trace.flowRefs`
- 依赖引用可解析
- 用户选择「执行校验」时，`npm run validate`（或 `--type api`）应通过

## 生成后交互

生成完成后须询问用户是否校验，不得默认自动执行。见 [01_工作流.md](01_工作流.md#校验确认生成后必做)。

## 输出

- `test-artifacts/api-cases/api-cases-{ts}.json`
