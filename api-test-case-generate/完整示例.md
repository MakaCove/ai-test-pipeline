# 完整示例（主流程模式）

## 输入

后端主流程：

- `POST /api/v1/auth/login` → token
- `POST /api/v1/orders` → orderId（body 需 `customerId`）
- `PUT /api/v1/orders/{id}/submit` → 提交审核

`CreateOrderDTO` 含 `customerId`、`sku`、`quantity`。

## 识别的主流程

| id | name | suggestedCaseIds |
|----|------|------------------|
| FLOW-AUTH-001 | 登录获取 token | TC-API-AUTH-001 |
| FLOW-ORDERS-001 | 创建并提交订单 | TC-API-WF-ORDERS-001, TC-API-WF-ORDERS-002 |

## 输出（节选）

```json
[
  {
    "schema": "ai-test-pipeline/api-case/v1",
    "generatedAt": "2026-05-29T10:00:00+08:00",
    "sourceAnalysis": "test-artifacts/project-analysis-20260529-095500.json",
    "totalCases": 3,
    "mockData": {
      "customerId": "MOCK-CUST-001",
      "productSku": "SKU-DEMO-01"
    },
    "coverage": {
      "mainFlowTotal": 2,
      "mainFlowsCovered": 2,
      "mainFlowCoveragePercent": 100,
      "uncoveredMainFlows": [],
      "orphanCases": 0,
      "mainFlowInventory": [
        {
          "id": "FLOW-AUTH-001",
          "name": "登录获取 token",
          "module": "auth",
          "priority": "P0",
          "businessGoal": "获取后续 API 鉴权 token",
          "endpointSequence": ["POST /api/v1/auth/login"],
          "evidence": ["AuthController.login"],
          "suggestedCaseIds": ["TC-API-AUTH-001"]
        },
        {
          "id": "FLOW-ORDERS-001",
          "name": "创建并提交订单",
          "module": "orders",
          "priority": "P0",
          "businessGoal": "用户创建订单并提交审核",
          "endpointSequence": [
            "POST /api/v1/orders",
            "PUT /api/v1/orders/{id}/submit"
          ],
          "evidence": ["OrderController.create", "OrderController.submit"],
          "suggestedCaseIds": ["TC-API-WF-ORDERS-001", "TC-API-WF-ORDERS-002"],
          "mockHints": ["customerId ← mockData", "sku ← mockData.productSku"]
        }
      ]
    }
  },
  {
    "id": "TC-API-AUTH-001",
    "flowRef": "FLOW-AUTH-001",
    "module": "auth",
    "title": "主流程 - 登录获取 token",
    "priority": "P0",
    "executionOrder": 1,
    "request": {
      "method": "POST",
      "path": "/api/v1/auth/login",
      "headers": { "Content-Type": "application/json" },
      "body": { "username": "{{username}}", "password": "{{password}}" }
    },
    "assertions": [
      { "field": "status", "operator": "eq", "expected": 200 },
      { "field": "body.code", "operator": "eq", "expected": 0 },
      { "field": "body.data.token", "operator": "exists", "expected": null }
    ],
    "extract": { "token": "body.data.token" },
    "produces": ["token"]
  },
  {
    "id": "TC-API-WF-ORDERS-001",
    "flowRef": "FLOW-ORDERS-001",
    "module": "orders",
    "title": "主流程 - 创建订单",
    "priority": "P0",
    "executionOrder": 2,
    "dependsOnCases": ["TC-API-AUTH-001"],
    "consumes": ["token"],
    "produces": ["orderId"],
    "mockData": { "quantity": 1 },
    "request": {
      "method": "POST",
      "path": "/api/v1/orders",
      "headers": { "Authorization": "Bearer {{token}}", "Content-Type": "application/json" },
      "body": {
        "customerId": "{{customerId}}",
        "sku": "{{productSku}}",
        "quantity": "{{quantity}}"
      }
    },
    "assertions": [
      { "field": "status", "operator": "eq", "expected": 200 },
      { "field": "body.code", "operator": "eq", "expected": 0 },
      { "field": "body.data.id", "operator": "exists", "expected": null }
    ],
    "extract": { "orderId": "body.data.id" }
  },
  {
    "id": "TC-API-WF-ORDERS-002",
    "flowRef": "FLOW-ORDERS-001",
    "module": "orders",
    "title": "主流程 - 提交订单审核",
    "priority": "P0",
    "executionOrder": 3,
    "dependsOnCases": ["TC-API-WF-ORDERS-001"],
    "consumes": ["token", "orderId"],
    "request": {
      "method": "PUT",
      "path": "/api/v1/orders/{{orderId}}/submit",
      "headers": { "Authorization": "Bearer {{token}}" }
    },
    "assertions": [
      { "field": "status", "operator": "eq", "expected": 200 },
      { "field": "body.code", "operator": "eq", "expected": 0 }
    ]
  }
]
```

## 说明

- 仅 **3 条用例**覆盖 2 条主流程；未单独写校验失败、分页、401 变体。
- `customerId`/`productSku` 来自元信息 `mockData`；`quantity` 来自用例级 mock。
- 执行器启动时注入 `mockData`，再跑登录与 WF 链路。
