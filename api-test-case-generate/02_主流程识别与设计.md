# 主流程识别与设计（API）

## 目标

将 `project-analysis.mainFlowCandidates` 转化为 API 用例主流程清单，并产出可执行用例。

## 设计原则

- 主流程覆盖优先于用例数量
- 每个用例必须绑定 `trace.flowRefs`
- 链路依赖使用 `trace.dependsOn` / `trace.produces` / `trace.consumes`
