# 05 — 完整示例

## 输入

假设后端有一个 `AuthController`：

```java
@RestController
@RequestMapping("/api/v1/auth")
public class AuthController {
    @PostMapping("/login")
    public Result<LoginVO> login(@RequestBody @Valid LoginDTO dto) { ... }

    @GetMapping("/me")
    public Result<UserVO> me(@RequestHeader("Authorization") String token) { ... }
}
```

## 输出

基于以上控制器生成的完整 JSON 用例文件：

```json
[
  {
    "schema": "ai-test-pipeline/api-case/v1",
    "generatedAt": "2026-05-28T14:23:00+08:00",
    "totalCases": 5
  },
  {
    "id": "TC-API-AUTH-001",
    "module": "auth",
    "title": "用户登录 - 正常登录",
    "description": "使用有效凭证登录，验证返回 token 和用户信息。",
    "priority": "P0",
    "tags": ["冒烟测试", "认证"],
    "preconditions": ["数据库中存在用户 admin/123admin"],
    "request": {
      "method": "POST",
      "path": "/api/v1/auth/login",
      "headers": { "Content-Type": "application/json" },
      "body": { "username": "admin", "password": "123admin" }
    },
    "assertions": [
      { "field": "status", "operator": "eq", "expected": 200 },
      { "field": "body.data.token", "operator": "exists", "expected": null },
      { "field": "body.data.user.id", "operator": "exists", "expected": null }
    ],
    "extract": {
      "token": "body.data.token",
      "userId": "body.data.user.id"
    }
  },
  {
    "id": "TC-API-AUTH-002",
    "module": "auth",
    "title": "用户登录 - 密码错误",
    "description": "使用错误密码登录，验证返回 401。",
    "priority": "P1",
    "tags": ["认证", "异常用例"],
    "preconditions": [],
    "request": {
      "method": "POST",
      "path": "/api/v1/auth/login",
      "headers": { "Content-Type": "application/json" },
      "body": { "username": "admin", "password": "wrongpassword" }
    },
    "assertions": [
      { "field": "status", "operator": "eq", "expected": 401 }
    ]
  },
  {
    "id": "TC-API-AUTH-003",
    "module": "auth",
    "title": "用户登录 - 缺少用户名",
    "description": "请求体中缺少 username 字段，验证返回 400。",
    "priority": "P1",
    "tags": ["认证", "参数校验"],
    "preconditions": [],
    "request": {
      "method": "POST",
      "path": "/api/v1/auth/login",
      "headers": { "Content-Type": "application/json" },
      "body": { "password": "123admin" }
    },
    "assertions": [
      { "field": "status", "operator": "eq", "expected": 400 }
    ]
  },
  {
    "id": "TC-API-AUTH-004",
    "module": "auth",
    "title": "获取当前用户信息 - 正常请求",
    "description": "使用有效的 Bearer token 获取当前用户信息，验证返回正确的用户名。",
    "priority": "P0",
    "tags": ["冒烟测试", "认证"],
    "preconditions": ["已完成登录获取 token"],
    "request": {
      "method": "GET",
      "path": "/api/v1/auth/me",
      "headers": { "Authorization": "Bearer {{token}}" }
    },
    "assertions": [
      { "field": "status", "operator": "eq", "expected": 200 },
      { "field": "body.data.username", "operator": "eq", "expected": "admin" }
    ]
  },
  {
    "id": "TC-API-AUTH-005",
    "module": "auth",
    "title": "获取当前用户信息 - 无 Token",
    "description": "不携带 token 请求 `/auth/me`，验证返回 401。",
    "priority": "P1",
    "tags": ["认证", "异常用例"],
    "preconditions": [],
    "request": {
      "method": "GET",
      "path": "/api/v1/auth/me",
      "headers": {}
    },
    "assertions": [
      { "field": "status", "operator": "eq", "expected": 401 }
    ]
  }
]
```
