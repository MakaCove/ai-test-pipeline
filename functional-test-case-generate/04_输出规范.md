# 04 — 输出规范与文件命名

## 输出文件

- **路径**：`test-artifacts/functional-cases/functional-cases-{yyyyMMdd-HHmmss}.json`
- **编码**：UTF-8
- **格式化**：2 空格缩进

## 文件结构

**Canonical Schema**：`_shared/功能用例结构.md`（含 `featureInventory`、`featureRef`、`featureRefs`）。下文为摘要示例（字段可截断，语义不可缺失）：

```json
[
  {
    "schema": "ai-test-pipeline/functional-case/v1",
    "generatedAt": "2026-05-28T14:23:00+08:00",
    "baseUrl": "http://localhost:5173",
    "sourceAnalysis": "test-artifacts/project-analysis-20260528-154821.json",
    "totalCases": 52,
    "coverage": {
      "routeTotal": 12,
      "routesCovered": 12,
      "routeCoveragePercent": 100,
      "featureTotal": 48,
      "featuresCovered": 48,
      "featureCoveragePercent": 100,
      "uncoveredFeatures": [],
      "byModule": { "auth": 6, "orders": 11 },
      "byPriority": { "P0": 38, "P1": 14 },
      "byDesignMethod": {
        "等价类": 14,
        "用例场景": 4,
        "错误推测": 6
      },
      "designMethodsCovered": 6,
      "featureInventory": [
        {
          "id": "FEAT-AUTH-001",
          "route": "/login",
          "module": "auth",
          "type": "form",
          "name": "用户名密码登录",
          "priority": "P0"
        }
      ]
    }
  },
  {
    "id": "TC-FUNC-AUTH-001",
    "featureRef": "FEAT-AUTH-001",
    "module": "auth",
    "title": "认证 - 用户名密码登录 - 合法凭证进入首页",
    "steps": [
      { "step": 1, "action": "navigate", "target": "/login", "value": "" },
      { "step": 2, "action": "fill", "target": "用户名输入框", "value": "{{username}}" },
      { "step": 3, "action": "fill", "target": "密码输入框", "value": "{{password}}" },
      { "step": 4, "action": "click", "target": "登录按钮", "value": "" }
    ],
    "assertions": [
      { "type": "url_contains", "target": "/dashboard", "expected": true },
      { "type": "element_visible", "target": "用户菜单或头像", "expected": true }
    ]
  }
]
```

> 注意：实际产物中 `steps` 与 `assertions` 不得为空；若示例为摘要截断，需在文档明确标注。

### 元信息说明

| 字段 | 说明 |
|------|------|
| `totalCases` | 实际用例数（结果值，非目标 KPI） |
| `featureInventory` | 功能清单，生成用例的依据 |
| `featureCoveragePercent` | P0 功能点覆盖率，验收目标 100% |
| `uncoveredFeatures` | 未覆盖功能 id 列表，应为 `[]` |

**已废弃**：`targetMinCases` 及任何「条数下限」硬性字段。

## 用例扩展字段

| 字段 | 说明 |
|------|------|
| `featureRef` | 单功能点 id |
| `featureRefs` | 流程用例涉及的多个功能点 |
| `relatedApis` | 可选，便于与接口用例对照 |

## latest 指针

更新 `test-artifacts/latest` 为当前时间戳（与接口用例共用时可覆盖）。

## 生成后确认（对话摘要）

```
✅ 功能测试用例已生成

- 📁 文件：test-artifacts/functional-cases/functional-cases-{ts}.json
- 📊 用例数：52（随功能复杂度自然得出，非 KPI）
- 🧭 路由覆盖：12/12（100%）
- 🎯 功能点覆盖：48/48 P0（100%）
- 📦 模块分布：auth(6)、orders(11)、...
- 🧪 设计方法：6 种（按功能点选用）
- 📋 清单：featureInventory 已写入 meta
```

**不要**在摘要中写「未达 70 条未达标」类表述。
