# 完整示例（主流程模式）

> 示例仅说明结构，不绑定具体业务。

## 输入

项目有：

- `/login`：认证页
- `/orders`：订单列表，可「新建订单」
- `/orders/create`：创建表单，提交后回列表

侧栏菜单：`订单管理` → `/orders`

`chainHints` 含订单创建生命周期。

## 识别的主流程

| id | name | suggestedCaseIds |
|----|------|------------------|
| FLOW-AUTH-001 | 登录进入系统 | TC-FUNC-AUTH-001 |
| FLOW-ORDERS-001 | 创建订单并在列表可见 | TC-FUNC-ORDERS-001 |

## 输出（节选）

```json
[
  {
    "schema": "ai-test-pipeline/functional-case/v1",
    "generatedAt": "2026-05-29T10:00:00+08:00",
    "baseUrl": "http://localhost:5173",
    "navigationModel": "menu-driven",
    "sourceAnalysis": "test-artifacts/project-analysis-20260529-095500.json",
    "totalCases": 2,
    "coverage": {
      "mainFlowTotal": 2,
      "mainFlowsCovered": 2,
      "mainFlowCoveragePercent": 100,
      "uncoveredMainFlows": [],
      "orphanCases": 0,
      "routeMenuMap": [
        { "route": "/orders", "menuLabel": "订单管理", "requiresAuth": true },
        { "route": "/login", "menuLabel": null, "navigation": { "type": "direct" }, "requiresAuth": false }
      ],
      "mainFlowInventory": [
        {
          "id": "FLOW-AUTH-001",
          "name": "登录进入系统",
          "module": "auth",
          "priority": "P0",
          "userGoal": "用户使用合法凭证登录并进入业务页",
          "stepsSummary": "打开登录页 → 填表 → 提交 → 进入订单模块",
          "evidence": ["LoginView.vue: 登录按钮", "POST /api/v1/auth/login"],
          "suggestedCaseIds": ["TC-FUNC-AUTH-001"],
          "confidence": "high"
        },
        {
          "id": "FLOW-ORDERS-001",
          "name": "创建订单并在列表可见",
          "module": "orders",
          "priority": "P0",
          "userGoal": "业务用户新建一条订单并在列表中看到",
          "menuPath": ["订单管理"],
          "stepsSummary": "菜单进入订单 → 新建 → 填表提交 → 列表出现新记录",
          "evidence": ["OrdersView.vue: 新建订单", "OrderForm.vue: POST /api/v1/orders"],
          "suggestedCaseIds": ["TC-FUNC-ORDERS-001"],
          "chainVars": ["orderTitle"],
          "confidence": "high"
        }
      ]
    }
  },
  {
    "id": "TC-FUNC-AUTH-001",
    "title": "登录进入系统",
    "module": "auth",
    "priority": "P0",
    "trace": {
      "flowRefs": ["FLOW-AUTH-001"],
      "produces": ["sessionUser"]
    },
    "steps": [
      { "step": 1, "action": "reset_session" },
      { "step": 2, "action": "open_login" },
      { "step": 3, "action": "fill", "target": "用户名", "value": "{{username}}" },
      { "step": 4, "action": "fill", "target": "密码", "value": "{{password}}" },
      { "step": 5, "action": "click", "target": "登录" },
      { "step": 6, "action": "wait", "value": "1200" }
    ],
    "assertions": [
      { "type": "url_contains", "target": "/orders", "expected": true },
      { "type": "element_visible", "target": "订单管理", "expected": true }
    ]
  },
  {
    "id": "TC-FUNC-ORDERS-001",
    "title": "创建订单并在列表可见",
    "module": "orders",
    "priority": "P0",
    "trace": {
      "flowRefs": ["FLOW-ORDERS-001"],
      "dependsOn": ["TC-FUNC-AUTH-001"],
      "produces": ["orderTitle"],
      "consumes": ["sessionUser"]
    },
    "steps": [
      { "step": 1, "action": "ensure_session" },
      { "step": 2, "action": "click_menu", "target": "订单管理" },
      { "step": 3, "action": "wait", "value": "1000" },
      { "step": 4, "action": "click", "target": "新建订单" },
      { "step": 5, "action": "fill", "target": "订单标题", "value": "AUTO-{{timestamp}}", "saveAs": "orderTitle" },
      { "step": 6, "action": "click", "target": "提交" },
      { "step": 7, "action": "wait", "value": "1500" }
    ],
    "assertions": [
      { "type": "url_contains", "target": "/orders", "expected": true },
      { "type": "text_contains", "target": "AUTO-", "expected": true }
    ]
  }
]
```

## 说明

- 仅 **2 条用例**覆盖 2 条主流程；未单独写「分页」「校验失败」「筛选」用例。
- `TC-FUNC-ORDERS-001` 把创建到列表可见串成一条端到端路径。
- 所有链路字段统一在 `trace` 对象中，追溯用 `trace.flowRefs`。
- 若还有「提交审核」等后续主流程，再增 `FLOW-ORDERS-002` + `TC-FUNC-ORDERS-002`。
