# 验收与输出（UI）

## 验收

- Schema 为 `ai-test-pipeline/ui-case/v2`
- 每条用例包含 `trace.flowRefs`
- `steps` 与 `assertions` 非空
- 用户选择「执行校验」时，校验脚本应通过

## 生成后交互

生成完成后须询问用户是否校验，不得默认自动执行。见 [01_工作流.md](01_工作流.md#校验确认生成后必做)。

## 输出

- `test-artifacts/ui-cases/ui-cases-{ts}.json`
