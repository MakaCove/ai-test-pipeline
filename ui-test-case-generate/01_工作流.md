# 工作流（UI 用例生成）

1. 读取 `project-analysis`
2. 确认 UI 主流程清单
3. 产出 `routeMenuMap`
4. 生成 UI 用例 JSON
5. 运行 `npm run validate`

输出目录：`test-artifacts/ui-cases/`

Schema 见 [shared/05_UI用例结构.md](../shared/05_UI用例结构.md)。
